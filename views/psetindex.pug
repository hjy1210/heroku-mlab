extends layout

block content2
  //p Welcome to #[em Psetbank], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.

  //h1 Dynamic content

  //if error
  //  p Error getting dynamic content.
  //else
  //  p The library has the following record counts:
  //
  //  ul
  //    li #[strong Psets:] !{data.pset_count}
  if psets
    ul
      each pset in psets
        li 
          a(href=pset.url) #{pset.code}
          if pset.media && pset.media.length>0
            //each item in pset.media
            //  span (#{item.filename})
  else
    li There are no books.
