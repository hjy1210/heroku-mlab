extends layout

block content
  h1 Problem Set list
  
  if psets && psets.length>0
    ul
      each pset in psets
        li 
          a(href=pset.url) #{pset.code}
          if pset.mediacount>0
            span (#{pset.mediacount})
            each item in pset.media
              span (#{item.filename})
  else
    p There are no psets.
  